# اختبار Laravel - مشروع السينما 🎬

## معلومات الاختبار
- **المدة:** 3 ساعات
- **النقاط الإجمالية:** 100 نقطة
- **المراجع المسموحة:** Laravel Documentation فقط

---

## الجزء الأول: الأسئلة النظرية (30 نقطة)

### 1. مفاهيم Laravel الأساسية (10 نقاط)

#### أ) اشرح الفرق بين:
- `@extends` و `@include` في Blade
- `Route::get()` و `Route::post()`
- `Auth::guard('client')` و `Auth::guard('admin')`

#### ب) ما هو الهدف من:
- Middleware في Laravel؟
- Service Provider؟
- Eloquent Relationships؟

### 2. قاعدة البيانات (10 نقاط)

#### أ) اكتب العلاقات التالية في Models:
```php
// في Movie Model
// علاقة مع Category (Many-to-Many)
// علاقة مع MovieShow (One-to-Many)

// في Booking Model  
// علاقة مع Client (Many-to-One)
// علاقة مع MovieShow (Many-to-One)
```

#### ب) اكتب Query Builder للحصول على:
- جميع الأفلام النشطة مع فئاتها
- الحجوزات في آخر 7 أيام
- المسارح التي لديها أكثر من شاشة واحدة

### 3. الأمان والتحقق (10 نقاط)

#### أ) اشرح كيفية:
- حماية النماذج من CSRF
- التحقق من صحة البيانات المدخلة
- التحكم في الوصول للمستخدمين

#### ب) ما هي أفضل الممارسات لـ:
- تخزين كلمات المرور؟
- إدارة الجلسات؟
- حماية API endpoints؟

---

## الجزء الثاني: البرمجة العملية (70 نقطة)

### 1. إنشاء Controller جديد (15 نقطة)

قم بإنشاء `ReportController` يحتوي على:
- دالة لعرض تقرير المبيعات اليومية
- دالة لعرض أكثر الأفلام شعبية
- دالة لتصدير البيانات إلى Excel

```php
// المطلوب:
class ReportController extends Controller
{
    public function dailySales()
    {
        // كود لعرض المبيعات اليومية
    }
    
    public function popularMovies()
    {
        // كود لعرض أكثر الأفلام شعبية
    }
    
    public function exportToExcel()
    {
        // كود لتصدير البيانات
    }
}
```

### 2. إنشاء Migration جديدة (10 نقاط)

قم بإنشاء migration لإضافة جدول `reviews` للأفلام:

```php
// المطلوب:
- id (primary key)
- movie_id (foreign key)
- client_id (foreign key) 
- rating (1-5)
- comment (text)
- created_at, updated_at
```

### 3. إنشاء Model وعلاقاته (10 نقاط)

```php
// Movie Model - أضف علاقة مع Reviews
// Review Model - أضف علاقات مع Movie و Client
// Client Model - أضف علاقة مع Reviews
```

### 4. إنشاء Routes (5 نقاط)

```php
// أضف Routes للـ ReportController
// أضف Routes للـ Reviews
// تأكد من حماية Routes المناسبة
```

### 5. إنشاء Views (15 نقطة)

#### أ) إنشاء صفحة تقرير المبيعات (10 نقاط)
```blade
{{-- المطلوب: --}}
{{-- - جدول يعرض المبيعات اليومية --}}
{{-- - رسوم بيانية (يمكن استخدام Chart.js) --}}
{{-- - فلترة حسب التاريخ --}}
{{-- - زر تصدير Excel --}}
```

#### ب) إنشاء نموذج إضافة تقييم (5 نقاط)
```blade
{{-- المطلوب: --}}
{{-- - نموذج مع 5 نجوم للتقييم --}}
{{-- - حقل نصي للتعليق --}}
{{-- - التحقق من صحة البيانات --}}
```

### 6. إنشاء JavaScript (10 نقاط)

#### أ) إضافة تفاعلية لصفحة التقييم (5 نقاط)
```javascript
// المطلوب:
// - تفاعل مع نجوم التقييم
// - عرض التقييم المختار
// - إرسال البيانات عبر AJAX
```

#### ب) إضافة رسوم بيانية (5 نقاط)
```javascript
// المطلوب:
// - رسم بياني للمبيعات اليومية
// - رسم بياني لأكثر الأفلام شعبية
// - استخدام Chart.js أو أي مكتبة أخرى
```

### 7. إنشاء Validation Rules (5 نقاط)

```php
// المطلوب:
// - إنشاء Request Class للـ Review
// - قواعد التحقق المناسبة
// - رسائل خطأ مخصصة
```

---

## الجزء الثالث: حل المشاكل (اختياري - 20 نقطة إضافية)

### المشكلة:
لديك مشكلة في نظام الحجز حيث:
- المستخدمين يشكون من بطء الصفحة
- أحياناً يتم حجز نفس المقعد مرتين
- لا يوجد تأكيد بالبريد الإلكتروني

### المطلوب:
1. **تحسين الأداء (10 نقاط)**
   - استخدم Caching للبيانات المتكررة
   - optimize database queries
   - استخدم pagination للقوائم الطويلة

2. **حل مشكلة الحجز المزدوج (5 نقاط)**
   - استخدم database transactions
   - أضف unique constraints
   - implement optimistic locking

3. **إضافة تأكيد البريد الإلكتروني (5 نقاط)**
   - إنشاء email templates
   - إرسال تأكيد الحجز
   - إضافة رابط إلغاء الحجز

---

## معايير التقييم

### التقييم العام:
- **90-100:** ممتاز - فهم عميق ومهارات عالية
- **80-89:** جيد جداً - فهم جيد مع بعض الأخطاء البسيطة
- **70-79:** جيد - فهم أساسي مع أخطاء متوسطة
- **60-69:** مقبول - فهم محدود مع أخطاء كثيرة
- **أقل من 60:** يحتاج تحسين - فهم ضعيف

### معايير محددة:
- **جودة الكود:** 20%
- **الأمان:** 20%
- **الأداء:** 15%
- **التوثيق:** 10%
- **حل المشاكل:** 35%

---

## نصائح للاختبار

1. **اقرأ السؤال بعناية** قبل البدء في الإجابة
2. **خطط للإجابة** قبل الكتابة
3. **راجع الكود** قبل تسليمه
4. **اختبر الوظائف** إذا كان ممكناً
5. **اكتب تعليقات** توضيحية للكود المعقد

---

## وقت البدء: _________
## وقت الانتهاء: _________

**حظاً موفقاً! 🍀** 