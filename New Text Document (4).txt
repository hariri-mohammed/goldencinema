    <nav x-data="{ open: false, searchOpen: false }" class="bg-main-color border-b border-gray-100 dark:border-gray-700">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="relative flex items-center justify-between h-16">
                <!-- الجانب الأيسر (الشعار + الروابط) -->
                <div class="flex-1 flex items-center justify-start">
                    <!-- روابط التنقل -->
                    <div class="hidden sm:flex sm:space-x-6 ml-4">
                        <x-nav-link href="{{ route('managerdashboard') }}"
                            class="text-white hover:text-second-color transition-colors duration-200 font-semibold">
                            {{ __('Home') }}
                        </x-nav-link>
                        <x-nav-link href="{{ route('movie.index') }}"
                            class="text-white hover:text-second-color transition-colors duration-200 font-semibold">
                            {{ __('Movies') }}
                        </x-nav-link>
                    </div>
                </div>

                <!-- الشعار في المنتصف -->
                <div class="flex-1 flex items-center justify-center">
                    <div class="logo transform hover:scale-105 transition-transform duration-200">
                        <img src="{{ asset('img/Logo.PNG') }}" alt="Logo" class="h-14">
                    </div>
                </div>

                <!-- الجانب الأيمن (بحث، ملف شخصي، تسجيل خروج) -->
                <div class="flex-1 flex items-center justify-end space-x-4">
                    <!-- نظام البحث -->
                    <div class="relative">
                        <!-- زر البحث -->
                        <button @click="searchOpen = !searchOpen"
                            class="p-2 text-second-color hover:text-darker-color transition-colors">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                            </svg>
                        </button>

                        <!-- شريط البحث -->
                        <div x-show="searchOpen" @click.away="searchOpen = false"
                            class="absolute right-0 mt-2 w-64 bg-white rounded-lg shadow-lg p-2" style="display: none;">
                            <div class="flex">
                                <input type="text"
                                    class="flex-1 px-3 py-2 rounded-l-lg focus:outline-none focus:ring-2 focus:ring-second-color"
                                    placeholder="Search...">
                                <button
                                    class="px-4 py-2 bg-second-color text-white rounded-r-lg hover:bg-darker-color transition-colors">
                                    Search
                                </button>
                            </div>
                        </div>
                    </div>

                    <!-- ملف شخصي -->
                    <a href="#" class="p-2 text-white hover:text-second-color transition-colors hidden sm:block">
                        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                        </svg>
                    </a>

                    <!-- تسجيل الخروج -->
                    <form method="POST" action="{{ route('managerlogout') }}" class="hidden sm:block">
                        @csrf
                        <button type="submit"
                            class="bg-second-color hover:bg-darker-color text-white px-4 py-2 rounded-lg transition-colors duration-200 font-semibold">
                            {{ __('Log Out') }}
                        </button>
                    </form>

                    <!-- قائمة الجوال -->
                    <div class="sm:hidden">
                        <button @click="open = !open" class="p-2 text-white hover:text-second-color">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path :class="{ 'hidden': open, 'inline-flex': !open }" stroke-linecap="round"
                                    stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                                <path :class="{ 'hidden': !open, 'inline-flex': open }" stroke-linecap="round"
                                    stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                            </svg>
                        </button>
                    </div>
                </div>
            </div>

            <!-- القائمة المنسدلة للجوال -->
            <div class="sm:hidden" x-show="open" @click.away="open = false">
                <div class="px-2 pt-2 pb-3 space-y-1">
                    <!-- البحث في الجوال -->
                    <div class="mb-4">
                        <input type="text"
                            class="w-full px-3 py-2 rounded-lg focus:outline-none focus:ring-2 focus:ring-second-color"
                            placeholder="Search...">
                    </div>

                    <x-responsive-nav-link href="{{ route('managerdashboard') }}"
                        class="text-white hover:bg-second-color hover:text-white">
                        {{ __('Home') }}
                    </x-responsive-nav-link>

                    <x-responsive-nav-link href="{{ route('movie.index') }}"
                        class="text-white hover:bg-second-color hover:text-white">
                        {{ __('Movies') }}
                    </x-responsive-nav-link>

                    <!-- ملف شخصي -->
                    <a href="#"
                        class="block px-3 py-2 text-white hover:bg-second-color hover:text-white rounded-lg">
                        <div class="flex items-center">
                            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                            </svg>
                            Profile
                        </div>
                    </a>

                    <!-- تسجيل الخروج -->
                    <form method="POST" action="{{ route('managerlogout') }}">
                        @csrf
                        <button type="submit"
                            class="w-full text-left px-3 py-2 text-white hover:bg-second-color hover:text-white rounded-lg">
                            {{ __('Log Out') }}
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </nav>

    <style>
        :root {
            --main-color: #1e222efd;
            --second-color: #ff9e1b;
            --darker-color: #e4e43a;
        }

        .bg-main-color {
            background-color: var(--main-color);
        }

        .bg-second-color {
            background-color: var(--second-color);
        }

        .bg-darker-color {
            background-color: var(--darker-color);
        }

        .text-second-color {
            color: var(--second-color);
        }

        .text-darker-color {
            color: var(--darker-color);
        }

        .hover\:bg-second-color:hover {
            background-color: var(--second-color);
        }

        .hover\:bg-darker-color:hover {
            background-color: var(--darker-color);
        }

        .focus\:ring-second-color:focus {
            ring-color: var(--second-color);
        }
    </style>